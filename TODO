TopPrio:

* kernel:
	1. error regulator (new OVERO?)

* Networkmanager broken
	1. missing link in /etc/systemd/system/dbus-org.freedesktop.Networkmanager.service?
		ln -s '/lib/systemd/system/NetworkManager.service' '/etc/systemd/system/dbus-org.freedesktop.NetworkManager.service'
		ln -s '/lib/systemd/system/NetworkManager.service' '/etc/systemd/system/multi-user.target.wants/NetworkManager.service'
* xfce-4.10: 	
	1. No Background / no icons
	2. xfce4-notifyd: gold linker problem
	3. logoff-dlg: No shutdown (dbus?)
	4. recipes-panel-plugins/datetime/ patches upstream

* systemd:
	systemdctl-native:
		- echo for debug
		-
	# create the required symbolic 'Alias' links
	alias=$(grep Alias $ROOT/$service_file \
		        | sed 's,Alias=,,g' \
		        | tr ',' '\n' \
		        | grep '\.service$')

	for r in $alias; do
		if [ "$action" = "enable" ]; then
			mkdir -p $ROOT/etc/systemd/system
			ln -s $service_file $ROOT/etc/systemd/$r
			echo "Enabled $service for $alias."
		else
			rm -f $ROOT/etc/systemd/system/$r
			echo "Disabled $service for $alias."
		fi
	done
		- reverted patch
		- meta-systemd

	

* mpc / http://ftp.de.debian.org/debian/pool/main/m/mpd/

* systemd: *-systemd in tasks

* xfce-panel: static libs

* u-boot: 
  fix overo revision 1 -> mainline

* networkmanager fix: libnl-3 / wpa-supplicant / iw -> oe-core

* gvfs: crash gvfsd-smb-browse on first thunar start

* xfwm4 error on first start ( remove /home/root/.config )
	S98configure[285]: //var/lib/opkg/info/xfwm4.postinst: line 2: update-alternatives: command not found

* squeeze (xfce): needs thunar-vfs!! (git?)

* kernel: PARALLEL_MAKEINST is ignored see oe-core kernel.bbclass & meta-ti linux.inc




LowPrio

TODO XFCE image:

* https://launchpad.net/bamf/0.2/0.2.42

* xfce4-xfapplet-plugin ?

* xfce4-taskmanager

* xfce-panel-plugins libxfce4ui port -> mainline 
	cpufreq		https://bugzilla.xfce.org/show_bug.cgi?id=8051
	datetime	https://bugzilla.xfce.org/show_bug.cgi?id=8064
        eyes		https://bugzilla.xfce.org/show_bug.cgi?id=8074
        weather		https://bugzilla.xfce.org/show_bug.cgi?id=7956 (foreign)

* orage fix crash

* tumbler: split packages



TODO others:

* libgles: PROVIDES virtual/libgl ?

* evince: additional doctype support
# configure option  ->    current configure result
#-------------------------------------------------
# --enable-ps       ->    PostScript Backend.:  no
# --enable-djvu     ->    DJVU Backend.......:  no
# --enable-impress  ->    Impress Backend....:  no

* gobject-introspection

* mame
