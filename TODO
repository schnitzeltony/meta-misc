TopPrio:

* libgles: X11

* libsdl-omap

* kernel 3.8
	* USB host
	* warmstart old OMAP
	* WLAN
	* Power-off-button

* gvfs: open windows shared

* bluetooth:
       * udev rule for ttyO1 -> systemd.service -> hciattach ttyO1 csr

* geany-plugins fail at do_rootfs
	|  * satisfy_dependencies_for: Cannot satisfy the following dependencies for geany-plugins:
	|  * 	geany-plugins-devhelp * 	geany-plugins-webhelper *

* python-pygtk
	* readd pkgconfig
	* *.pyo for python examples not working

* gtk:
	* larger menus
	* force click in panel if e.g menu was opened

* lxdm:
	* select consolekit/systemd by packageconfig

* xfce4-settings:
	* do not handle gtk-touchscreen-mode persistent
	* on device remove: If there is at least one pointer device with cursor left over show it otherwise hide
	* remove hide option and detect touchscreen automatically (as in touchscreen calibration)
	* does not work for QT applications

* exo:
	* exo-open: startup-notification gets lost
	* build fails on some machines due to python modules

* thunar:
	* touchscreen long press -> open

* maliit:
	* hide when keyboard is available
	* qt apps don't work

* login-manager autostarts
	* maliit

* xfce4-taskmanager add


* xfce4-panel/window menu
	- don't display desktop
	- menu size
* xfce4-panel/starter
	- limit to one instance
	- open maximized
	- undecorate

* gimp: pdf import crashes (check for old poppler patch)

* /etc under git control

* kernel 3.8
  omap3_has_iva at board init

* kernel RTC backup charge

* kernel:
	* Bluetooth
	* OTG USB (temporary solution)
	* TWL4030 Watchdog?

xfce4-netload-plugin:
	Loading kernel module for a network device with CAP_SYS_MODULE (deprecated).  Use CAP_NET_ADMIN and alias netdev- instead

xfdesktop:
	fix dragging multiple items (see http://git.xfce.org/xfce/xfdesktop/commit/?id=578732b85aee34d0d48b4c16ac9ef398b644a9fc)

python-bindings broken for
	exo
	gnome-menus

* u-boot: 
  fix overo revision 1 -> mainline

* networkmanager fix: libnl-3 / wpa-supplicant / iw -> oe-core

* squeeze (xfce): needs thunar-vfs!! (git?)

* SWF: gnash / lightspark

* xfce4-xpugraph-plugin background


LowPrio

TODO XFCE image:

* xfce-panel-plugins libxfce4ui port -> mainline 
	cpufreq		https://bugzilla.xfce.org/show_bug.cgi?id=8051
        eyes		https://bugzilla.xfce.org/show_bug.cgi?id=8074

* tumbler: split packages



TODO others:

* evince: additional doctype support
# configure option  ->    current configure result
#-------------------------------------------------
# --enable-ps       ->    PostScript Backend.:  no
# --enable-djvu     ->    DJVU Backend.......:  no
# --enable-impress  ->    Impress Backend....:  no

* gobject-introspection

* mame
